<?xml version="1.0" encoding="utf-8"?>
<sumeru>
    <data>
        <!-- ir.ui.view : res.users (kanban) -->
        <view id="view_res_users_kanban" model="res.users" type="kanban">
            <field name="name" string="Name"/>
            <field name="login" string="Login"/>
            <field name="email" string="Email"/>
        </view>
    </data>
</sumeru>
