<?xml version="1.0" encoding="utf-8"?>
<sumeru>
    <data>
        <!-- sys.view : core.user (form) -->
        <view id="view_core.user_form" model="core.user" type="form">
            <header>
                <button name="action_archive" string="Archive" type="object" class=""/>
                <button name="action_reset_password" string="Send reset instructions" type="object" class="sum_highlight"/>
            </header>
            <sheet>
                <div class="sum_button_box" name="button_box">
                </div>
                <div class="sum_title">
                    <h1>
                        <field name="name" placeholder="e.g. Mitchell Admin"/>
                    </h1>
                    <div class="sum-title-contact-row">
                        <field name="email" string="Email" widget="email"/>
                        <field name="phone" string="Work phone"/>
                        <field name="mobile" string="Mobile"/>
                    </div>
                </div>

                <group>
                    <group string="Access">
                        <field name="login" string="Login"/>
                        <field name="active" string="Active"/>
                        <field name="user_type" string="User Type"/>
                    </group>
                    <group string="Company">
                        <field name="company_id" string="Company"/>
                    </group>
                </group>

                <notebook>
                    <page string="Preferences">
                        <group>
                            <group string="Localization">
                                <field name="lang" string="Language"/>
                                <field name="tz" string="Timezone"/>
                            </group>
                            <group string="Signature">
                                <field name="signature" string="Email signature"/>
                            </group>
                        </group>
                    </page>
                    <page string="Access Rights">
                        <!-- Groups checkboxes and password change are injected here by the renderer
                             via writeResUsersSecuritySection (html_renderer.go). This page acts as
                             the visual tab anchor. -->
                    </page>
                    <page string="Account Security">
                        <group>
                            <label for="login" string="Login is used to sign in. Keep it unique."/>
                            <field name="login" string="Login"/>
                        </group>
                    </page>
                </notebook>
            </sheet>
            <chatter>
                <field name="signature" string="Log" widget="mail_thread"/>
            </chatter>
        </view>
    </data>
</sumeru>
