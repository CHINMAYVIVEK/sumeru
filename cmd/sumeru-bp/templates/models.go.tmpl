// Package models contains the business domain objects for {{.Name}}.
package models

import (
	"sumeru/core/sdk"
)

// {{.TypeName}} is the primary record for {{.HumanTitle}}.
type {{.TypeName}} struct {
	sdk.Model `sumeru:"model={{.ModelDotted}}"`

	Name        sdk.String  `sumeru:"required,index,string=Name"`
	Description sdk.Text    `sumeru:"string=Description"`
	Active      sdk.Boolean `sumeru:"default=true,string=Active"`
	Sequence    sdk.Integer `sumeru:"default=10,string=Sequence"`
}
